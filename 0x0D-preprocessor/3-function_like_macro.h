#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/**
 * ABS - find absolute value of a number.
 * @x: number to find absolute of.
 * Return: 0(success)
 */

#define ABS(x) ((x) < 0 ? -(x) : (x))

#endif
