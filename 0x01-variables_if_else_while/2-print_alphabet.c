#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase.
 * Return:Always 0 (Success)
 */

int main(void)

{
	char bt;

	for (bt = 'a'; bt <= 'z'; bt++)
	putchar(bt);
	putchar('\n');
	return (0);
}
