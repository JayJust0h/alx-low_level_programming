#include <stdio.h>
/**
 * Main - Using the puts instead of printf command to output a function.
 * Return:0(Pass)
 */
int main(void);
{
puts("/"Programming is like building a multilingual puzzle");
return(0);
}
