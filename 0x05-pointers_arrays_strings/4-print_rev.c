#include "main.h"

/**
 * print_rev - Print string in reverse.
 * New line
 * @s: string to be printed.
 * Return: 0 (success)
 */

void print_rev(char *s)
{
	int longi = 0;
	int r
		while (*s != '\0')
		{	longi++
			s++
		} 
s--;
for (r = longi; r > 0; r--)
{
	_putchar (*s);
	s--;
}

	_putchar('\n')

}
