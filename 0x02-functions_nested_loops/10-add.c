#include "main.h"
/**
 * add - Function adds two integers and calculates the result
 * @j: integer to be added to f.
 * @f: integer to be added to j.
 * Return: sum of j and f.
 */
int add(int j, int f)
{
	int k;

	k = j + f;

	return (k);
}
